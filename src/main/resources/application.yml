micronaut:
  application:
    name: mongodbdemo
mongodb.uri: mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/mydb
netty:
  default:
    allocator:
      max-order: 3
db:
  name: 'fruit'
  collection: 'fruit'